🎯 Câu Lệnh Prompt Cho Phiên Làm Việc Kế Tiếp

  📋 Prompt Template:

  Tiếp tục CSS Architecture Foundation migration cho dự án tikz2svg_api. 
  Áp dụng methodology đã thiết lập:

  **MIGRATION RULES:**
  1. **Foundation First**: Luôn sử dụng variables từ master-variables.css
  2. **Conflict Resolution**: Loại bỏ duplicate html/body/:root rules
  3. **Scoping**: Thêm .tikz-app prefix cho tất cả selectors
  4. **Variable Conversion**: Thay hardcoded values → var(--variable-name)
  5. **Backup Safety**: Tạo .backup_migration trước khi edit
  6. **Responsive Consistency**: Maintain breakpoints với foundation variables
  7. **NO !important**: Tuyệt đối KHÔNG dùng !important, sử dụng higher specificity thay thế

  **NEXT PRIORITIES:**
  - file_card.css (core component)
  - navigation.css (global navigation)
  - search_results.css
  - view_svg.css

  **CSS BEST PRACTICES:**
  - ❌ NEVER use !important (except utilities like .hidden)
  - ✅ Use higher specificity: .tikz-app #id .class element
  - ✅ Bootstrap overrides: .tikz-app #container .component
  - ✅ Scope all styles with .tikz-app prefix
  - ✅ Use foundation variables for consistency
  
  **OUTPUT REQUIREMENT:**
  - Use TodoWrite tool để track progress
  - Document changes trong migration comments
  - Verify no conflicts: grep -rn ":root\|html.*{\|body.*{" static/css/
  - Check for !important usage: grep -rn "!important" static/css/

  🔍 Các Prompt Cụ Thể:

  1. Tiếp Tục File Migration:

  Thực hiện CSS Foundation migration cho file [FILE_NAME].css theo methodology
   đã thiết lập. 
  Tuân thủ: backup → remove conflicts → add scoping → replace variables → 
  test.

  2. Troubleshooting Migration:

  Kiểm tra và fix CSS conflicts trong dự án tikz2svg_api. 
  Tìm duplicate rules và convert về foundation system theo quy tắc đã thiết 
  lập.

  3. Component Enhancement:

  Cải thiện [COMPONENT_NAME] trong tikz2svg_api sử dụng foundation variables.
  Maintain glass morphism design và accessibility standards đã thiết lập.

  4. Responsive Optimization:

  Tối ưu responsive design cho [PAGE_NAME] sử dụng foundation breakpoint 
  system. 
  Ensure consistency với migration methodology.

  🎯 Key Context References:

  Foundation Files:

  - static/css/foundation/master-variables.css
  - static/css/foundation/global-base.css
  - templates/base.html (load order)

  Migration Status Tracking:

  - CSS_ARCHITECTURE_MIGRATION_STATUS.md
  - CSS_FOUNDATION_MIGRATION_SUMMARY.md

  Verified Migration Examples:

  - profile_svg_files.css ✅
  - profile_settings.css ✅
  - index.css ✅

  ⚡ Quick Start Commands:

  # Check remaining conflicts
  grep -rn ":root\|html.*{\|body.*{" static/css/ | grep -v foundation

  # Next priority files
  ls static/css/file_card.css static/css/navigation.css
  static/css/search_results.css

  🎨 Design System Variables:

  /* Colors */
  --primary-color, --text-on-glass, --glass-bg-strong

  /* Spacing */
  --spacing-4, --spacing-8, --spacing-16

  /* Glass Morphism */
  --glass-bg-light, --glass-blur-medium, --glass-shadow


  **********************
  Dựa trên những gì chúng ta đã thực hiện trong phiên này, đây là câu lệnh 
  prompt tối ưu để bắt đầu phiên làm việc mới:

  🎯 PROMPT TEMPLATE PHIÊN MỚI:

  Tiếp tục phát triển dự án tikz2svg_api theo CSS Foundation System 
  methodology đã thiết lập.

  **CONTEXT:**
  - Dự án: TikZ to SVG converter với Flask + MySQL + CSS Foundation System
  - Migration Status: 6/10 files completed (index.css, profile_*.css done)
  - Methodology: backup → remove conflicts → add scoping → replace variables →
   test

  **FOUNDATION SYSTEM:**
  - Variables: static/css/foundation/master-variables.css
  - Base: static/css/foundation/global-base.css  
  - Scoping: .tikz-app prefix required
  - Glass morphism: --glass-bg-*, --text-on-glass, accessibility ≥4.5:1

  **NEXT PRIORITIES:**
  1. file_card.css (core component used across pages)
  2. navigation.css (global navigation)
  3. search_results.css
  4. view_svg.css

  **TASK:** [Your specific request here]

  **METHODOLOGY RULES:**
  ✅ Use TodoWrite tool để track progress
  ✅ Backup trước khi edit: .backup_migration
  ✅ Remove duplicate html/body/:root rules
  ✅ Convert hardcoded → var(--variable-name)
  ✅ Test conflicts: grep -rn ":root\|html.*{\|body.*{" static/css/
  ✅ Document changes trong migration comments

  📝 PROMPT EXAMPLES CỤ THỂ:

  1. Tiếp Tục Migration:

  Tiếp tục CSS Foundation migration cho dự án tikz2svg_api. 
  Migrate file_card.css theo methodology đã thiết lập.

  2. Fix Issues:

  Fix CSS conflicts trong tikz2svg_api sử dụng Foundation System methodology.
  Kiểm tra và resolve duplicate rules.

  3. Component Enhancement:

  Cải thiện navigation component trong tikz2svg_api sử dụng foundation 
  variables.
  Maintain glass morphism design và accessibility standards.

  4. New Feature:

  Thêm [feature name] cho tikz2svg_api theo CSS Foundation System.
  Ensure consistency với migration methodology đã thiết lập.

  ⚡ QUICK START VARIANTS:

  Ngắn Gọn:

  Tiếp tục tikz2svg_api CSS Foundation migration - [specific task]

  Chi Tiết:

  tikz2svg_api: Continue CSS Foundation System work
  Context: 6/10 files migrated, next priority file_card.css
  Task: [your specific request]
  Method: Foundation variables + .tikz-app scoping + TodoWrite tracking

  🎯 RECOMMENDED STARTER:

  Tiếp tục phát triển tikz2svg_api theo CSS Foundation methodology.
  Migration progress: 6/10 files done, cần làm tiếp file_card.css.
  Task: [điền task cụ thể ở đây]
  Use TodoWrite để track progress.

Dựa trên CSS Foundation System và architecture đã thiết lập, đây là prompt 
  templates để phát triển trang mới tuân thủ chuẩn dự án:

  🎯 PROMPT TEMPLATES CHO TRANG MỚI:

  📄 1. TEMPLATE CHÍNH - TRANG MỚI:

  Tạo trang mới [PAGE_NAME] cho dự án tikz2svg_api tuân thủ architecture đã
  thiết lập.

  **FOUNDATION REQUIREMENTS:**
  ✅ Template inheritance: {% extends "base.html" %}
  ✅ CSS Foundation System: sử dụng master-variables.css
  ✅ Scoping: tất cả CSS với .tikz-app prefix
  ✅ Glass morphism: --glass-bg-*, --text-header-glass
  ✅ Responsive: foundation breakpoints
  ✅ Accessibility: contrast ≥ 4.5:1

  **FILE STRUCTURE:**
  - Template: templates/[page_name].html
  - CSS: static/css/[page_name].css  
  - JS: static/js/[page_name].js (nếu cần)
  - Route: thêm vào app.py

  **DESIGN SYSTEM:**
  - Colors: var(--primary-color), var(--text-on-glass)
  - Spacing: var(--spacing-*) (8px base)
  - Typography: var(--font-size-*), var(--font-weight-*)
  - Components: page-container, glass morphism containers

  **FUNCTIONALITY:** [Mô tả chức năng trang]

  Use TodoWrite để track development progress.

  🎨 2. TEMPLATE CHO UI/UX ENHANCEMENT:

  Thiết kế giao diện [PAGE_NAME] cho tikz2svg_api với modern UI/UX.

  **DESIGN STANDARDS:**
  ✅ Glass morphism design language
  ✅ Consistent với existing pages (index, profile)
  ✅ Mobile-first responsive approach
  ✅ Vietnamese language support
  ✅ CodeMirror/Quill.js integration nếu cần

  **COMPONENT REUSE:**
  - Navigation: include_navbar = true
  - File cards: include_file_card = true  
  - Modals: include_login_modal = true
  - Form elements: foundation form styles

  **ACCESSIBILITY:**
  - WCAG AA compliance
  - Keyboard navigation
  - Screen reader support
  - High contrast ratios

  **TASK:** [Chi tiết yêu cầu UI]

  ⚙️ 3. TEMPLATE CHO BACKEND INTEGRATION:

  Implement backend functionality cho trang [PAGE_NAME] trong tikz2svg_api.

  **FLASK REQUIREMENTS:**
  ✅ RESTful route conventions
  ✅ Database: parameterized queries
  ✅ Authentication: Google OAuth + Flask-Login
  ✅ Error handling: try-catch với logging
  ✅ Rate limiting: custom implementation

  **INTEGRATION POINTS:**
  - Database: MySQL models và queries
  - Email: Flask-Mail với Zoho SMTP nếu cần
  - File processing: TikZ pipeline nếu liên quan
  - Static files: unique naming conventions

  **SECURITY:**
  - Input validation & sanitization
  - CSRF protection
  - Environment variables từ .env
  - No hardcoded credentials

  **FUNCTIONALITY:** [Mô tả backend logic]

  🔧 4. TEMPLATE CHO FULL-STACK FEATURE:

  Phát triển tính năng [FEATURE_NAME] cho tikz2svg_api - full-stack
  implementation.

  **ARCHITECTURE COMPLIANCE:**
  ✅ Frontend: Jinja2 + CSS Foundation + Vanilla JS
  ✅ Backend: Flask + MySQL + proper error handling
  ✅ Design: Glass morphism + accessibility standards
  ✅ Performance: Optimized queries + caching

  **IMPLEMENTATION SCOPE:**
  1. Database schema changes (nếu cần)
  2. Flask routes + business logic
  3. Frontend templates + CSS Foundation
  4. JavaScript functionality (ES6+)
  5. Testing strategy

  **INTEGRATION:**
  - TikZ processing pipeline
  - User authentication system
  - File management system
  - Email notifications (nếu cần)

  **QUALITY GATES:**
  - Visual regression testing
  - Accessibility compliance
  - Mobile responsiveness
  - Performance benchmarks

  **FEATURE DESCRIPTION:** [Chi tiết tính năng]

  📋 SPECIFIC USE CASES:

  Trang Mới Đơn Giản:

  Tạo trang About cho tikz2svg_api với CSS Foundation System.
  Static content, glass morphism design, responsive layout.

  Trang Có Database:

  Tạo trang User Dashboard cho tikz2svg_api.
  Display user statistics, file management, glass morphism UI.
  Backend: MySQL queries, authentication required.

  Trang Có Form:

  Tạo trang Contact/Feedback cho tikz2svg_api.
  Form submission, email integration, validation, Foundation CSS.

  Admin/Management Page:

  Tạo Admin Panel cho tikz2svg_api.
  User management, system stats, authentication check.
  Restricted access, advanced UI components.

  🎯 RECOMMENDED STARTER:

  Phát triển [PAGE/FEATURE NAME] cho tikz2svg_api tuân thủ architecture.

  Foundation System: CSS variables + .tikz-app scoping
  Template: {% extends "base.html" %} inheritance  
  Design: Glass morphism + accessibility ≥ 4.5:1
  Backend: Flask + MySQL + proper error handling

  Chức năng: [mô tả chi tiết]
  Use TodoWrite tracking + document changes.

  📌 Thay [PAGE/FEATURE NAME] và [mô tả chi tiết] để bắt đầu development trang
   mới! 🚀

🧹 PROMPT TEMPLATE CHO CLEAN !IMPORTANT:

Clean up và tối ưu !important declarations trong dự án tikz2svg_api theo CSS Foundation methodology.

**CLEAN !IMPORTANT STRATEGY:**
✅ Audit: Tìm tất cả !important declarations
✅ Categorize: Phân loại necessary vs removable  
✅ Replace: Thay !important bằng higher specificity
✅ Test: Đảm bảo UI không bị break
✅ Document: Ghi lại changes và reasoning

**ANALYSIS APPROACH:**
1. **Critical !important**: Override vendor styles (CodeMirror, etc.) - GIỮ LẠI
2. **Utility !important**: Helper classes (.hidden, .text-center) - GIỮ LẠI  
3. **Conflict !important**: Fix CSS wars - REMOVE, thay bằng specificity
4. **Legacy !important**: Outdated overrides - REMOVE hoàn toàn

**REPLACEMENT METHODS:**
- Conflict: `.tikz-app .specific-class` thay vì `!important`
- Cascade: Đặt CSS ở vị trí sau trong load order
- Scope: Tăng specificity với parent selectors
- Variables: Sử dụng CSS custom properties

**TESTING REQUIREMENTS:**
- Visual regression: So sánh before/after screenshots
- Component isolation: Test từng component riêng biệt  
- Responsive: Test tất cả breakpoints
- Browser compatibility: Chrome, Firefox, Safari

**FILES TO AUDIT:**
- static/css/index.css (primary focus)
- static/css/foundation/global-base.css
- static/css/file_card.css
- static/css/navigation.css
- static/css/*profile*.css
- static/css/shared_variables.css

**OUTPUT FORMAT:**
```
BEFORE: 34 !important declarations
AFTER: X essential !important only

REMOVED CATEGORIES:
- Conflict overrides: X instances
- Legacy overrides: X instances  
- Redundant utilities: X instances

KEPT CATEGORIES:
- Vendor overrides: X instances (CodeMirror, etc.)
- Critical utilities: X instances (.hidden, etc.)
```

**METHODOLOGY:**
1. Backup: Tạo .backup_important trước khi edit
2. Audit: `grep -rn "!important" static/css/`
3. Categorize: Phân loại từng !important
4. Replace: Thay bằng specificity cao hơn
5. Test: Visual + functional testing
6. Document: Ghi lại trong migration comments

**TASK:** Clean up !important declarations để improve CSS architecture và maintainability.

Use TodoWrite để track progress từng file.